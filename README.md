# retail-orders-ingestion-pipeline
> Event-driven retail orders pipeline — Storage Event Trigger (ADF) → PySpark validation (Databricks) → Delta Lake audit log → Azure SQL DB, with cross-cloud ingestion from Amazon S3, schema/duplicate/status checks, Key Vault secret management, and parameterised pipeline execution on ADLS Gen2.
